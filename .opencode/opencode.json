{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "schema-migration": {
      "description": "Run GraphQL schema migration workflow",
      "agent": "plan",
      "template": "Create a migration plan first using the `schema-migration` skill, then execute the plan. Load and follow `.opencode/skills/schema-migration/SKILL.md`. Task: {{args}}"
    }
  }
}